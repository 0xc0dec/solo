* Move effect initialization into a separate method. Currently throwing exceptions from the constructor requires cleanup code to accompany each such "throw".
* Models loading
* Use position, rotation and scale to calculate the corresponding getter method results (instead of the matrix)
* Remove methods like multiply() from math classes