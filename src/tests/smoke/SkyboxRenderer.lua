--
-- Copyright (c) Aleksey Fedotov
-- MIT license
-- 

-- TODO Uncomment. Was commented because there's no way to create empty cube texture
-- local scene = sl.Scene.create(sl.device)
-- local r = scene:createNode():addSkyboxRenderer()
-- local tex = sl.CubeTexture.createEmpty(sl.device, 1, sl.TextureFormat.RGB)

-- r:setTexture(tex)
-- assert(r:getTexture())
-- r:setTexture(nil)