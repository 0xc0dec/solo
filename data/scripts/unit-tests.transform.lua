runTest(function()
	local v = solo.Vector3(1, 2, 3)
	local q = solo.Quaternion()
	local m = solo.Matrix()
	local scene = engine:getScene()
	local t = scene:createNode():findBuiltInComponent("Transform")
	local t2 = scene:createNode():findBuiltInComponent("Transform")
	t:setParent(t2)
	assert(t:getParent())
	assert(t2:getChild(0))
	assert(t2:getChildrenCount())
	t2:removeChildren()
	assert(t2:getWorldScale())
	assert(t2:getLocalScale())
	assert(t2:getWorldRotation())
	assert(t2:getLocalRotation())
	assert(t2:getWorldPosition())
	assert(t2:getLocalPosition())
	-- TODO
end, "Transform")
