runTest(function()
	local v = solo.Vector3(1, 2, 3)
	local q = solo.Quaternion()
	local m = solo.Matrix()
	local scene = engine:getScene()
	local t = scene:createNode():findBuiltInComponent("Transform")
	local t2 = scene:createNode():findBuiltInComponent("Transform")
	t:setParent(t2)
	assert(t:getParent())
	assert(t2:getChild(0))
	assert(t2:getChildrenCount())
	t2:removeChildren()
	assert(t:getWorldScale())
	assert(t:getLocalScale())
	assert(t:getWorldRotation())
	assert(t:getLocalRotation())
	assert(t:getWorldPosition())
	assert(t:getLocalPosition())
	assert(t:getLocalUp())
	assert(t:getLocalDown())
	assert(t:getLocalLeft())
	assert(t:getLocalRight())
	assert(t:getLocalForward())
	assert(t:getLocalBack())
	t:translateLocal(v)
	t:rotate(q, solo.TransformSpace_Self)
	t:rotateAxisAngle(v, 1, solo.TransformSpace_Self)
	t:scaleLocal(v)
	t:setLocalPosition(v)
	t:setLocalRotation(q)
	t:setLocalRotationAxisAngle(v, 1)
	t:setLocalScale(v)
	t:lookAt(v, v)
	assert(t:getMatrix())
	assert(t:getWorldMatrix())
	assert(t:getWorldViewMatrix()) -- TODO
	assert(t:getWorldViewProjectionMatrix()) -- TODO
	assert(t:getInverseTransposedWorldViewMatrix()) -- TODO
	assert(t:getInverseTransposedWorldMatrix())
	t:transformPoint(v)
	t:transformDirection(v)
end, "Transform")
